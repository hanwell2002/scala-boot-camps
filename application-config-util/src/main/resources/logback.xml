<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration>
<configuration>
    <variable name="LOG_FULL_PATH" value="/opt/bootcamps/log/application-config-example.log"/>
    <variable name="msg_pattern" value="%d{HH:mm:ss.SSS} App [%thread] %-5level %logger{36} - %msg%n"/>
    <variable name="message_pattern"
              value="%green(%d{ISO8601}) [%thread] %highlight(%-5level) [%blue(%t)] %yellow(%C{1.}): %msg%n%throwable"/>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_FULL_PATH}</file>
        <encoder>
            <pattern>${msg_pattern}</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${message_pattern}</pattern>
            </encoder>
        </appender>
    <appender name="ASYNCFILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
    </appender>

    <appender name="ASYNCSTDOUT" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="STDOUT"/>
    </appender>

    <logger name="application" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="ASYNCFILE"/>
        <appender-ref ref="ASYNCSTDOUT"/>
    </root>

</configuration>

